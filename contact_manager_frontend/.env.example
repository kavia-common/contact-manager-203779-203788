# Frontend configuration
# Request the orchestrator/user to set these in the real .env (do not commit .env)
# Base URL of the Express backend (no trailing slash). Example: http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001
